export interface IResponseData {
    code?: number;
    data?: any;
    message?: string;
    timestamp?: string;
    total?: number ;
}

export interface IListColor {
    code: number;
    name: string;
}